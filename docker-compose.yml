version: "3.9"

services:
  sentinel-ai:
    build: .
    container_name: sentinel-ai
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - sentinel_memory:/app/memory_data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  sentinel_memory:
